const Purchase = ({icon}) => (
    <button className="rounded-full bg-y w-[50px] h-[50px] flex justify-center items-center trans-200 hover:brightness-90">
        <img src={icon} alt="" />
    </button>
)

export default Purchase;